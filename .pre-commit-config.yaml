repos:
    # Mypy for static type checking
-   repo: https://github.com/pre-commit/mirrors-mypy
    rev: 'v0.991'
    hooks:
    -   id: mypy
        args: [--ignore-missing-imports, --follow-imports=skip, --disallow-untyped-defs, --exclude='/test_*.py']
